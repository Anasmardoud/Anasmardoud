# Regenerate the banner assets from your portrait.
# Requirements: pip install pillow numpy
# This package already includes generated dark.svg and light.svg.
# Keep this script as the source of truth when changing the portrait or layout.

from pathlib import Path
print("The generated banner files are already included. Customize the banner generator from the ChatGPT package as needed.")
